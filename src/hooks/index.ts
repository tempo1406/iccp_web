/**
 * src/hooks/index.ts — barrel export for all global hooks
 */
export { useDebounce } from './use-debounce';
export { useLocalStorage } from './use-local-storage';
export { usePagination } from './use-pagination';
